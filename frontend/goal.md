1. Create login/singup page
2. enable it with backend(so that mongodb can be used ot store the user)
3. create another page so that we can use authContext (routes) /-> if not login navigate to somether page
4. give a logout button 


/*
working fronted and backend 
we have to add 1:1 chat functionality
// select a use and send message
// Group chats
// read receipts -> this will cause the data model change
*/